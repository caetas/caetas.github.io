---
const navItems = [
  { label: 'Projects', href: '#projects' },
  { label: 'Publications', href: '#publications' },
  { label: 'Experience', href: '#experience' },
];
---
<header class="sticky top-0 z-30 w-full backdrop-blur border-b border-white/5 bg-slate-950/70">
  <div class="mx-auto flex w-full max-w-6xl items-center justify-between px-6 py-4">
    <a href="#top" class="group inline-flex items-center gap-3 text-white">
      <span class="grid h-10 w-10 place-items-center rounded-2xl bg-white text-lg font-semibold text-slate-950 transition-transform group-hover:scale-105">
        FC
      </span>
      <div class="flex flex-col text-sm leading-tight">
        <span class="font-semibold">Francisco Caetano</span>
        <span class="text-xs text-slate-400">Generative AI Researcher</span>
      </div>
    </a>
  <nav class="flex items-center gap-4 overflow-x-auto text-sm font-medium text-slate-400 md:gap-6">
      {navItems.map((item) => (
        <a
          href={item.href}
          class="rounded-full border border-transparent px-4 py-2 transition hover:border-white/10 hover:text-white"
        >
          {item.label}
        </a>
      ))}
    </nav>
    <a
      href="#contact"
      class="inline-flex items-center rounded-full bg-white px-4 py-2 text-sm font-semibold text-slate-900 transition hover:bg-slate-200"
    >
      Let's connect
    </a>
  </div>
</header>
