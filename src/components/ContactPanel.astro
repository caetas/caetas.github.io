---
import { profile, socialLinks } from '@data/profile';
---
<section id="contact" class="rounded-2xl border border-white/10 bg-slate-950/60 p-10 transition duration-300 hover:-translate-y-1 hover:border-white/25 hover:bg-slate-950/70">
  <div class="mx-auto max-w-3xl space-y-5 text-center">
    <p class="text-xs uppercase tracking-[0.4em] text-white/50">Contact</p>
    <h2 class="text-3xl font-semibold text-white">Let's collaborate thoughtfully</h2>
    <p class="text-sm text-white/70">
      I welcome conversations around applied generative research, reliable deployment, and interdisciplinary teams.
    </p>
    <div class="flex flex-wrap items-center justify-center gap-4 pt-2">
      <a
        href={`mailto:${profile.contact.email}`}
        class="inline-flex items-center gap-2 rounded-full bg-white px-6 py-2 text-sm font-semibold text-slate-900 transition hover:bg-slate-200"
      >
        Email Francisco
        <span aria-hidden="true">↗</span>
      </a>
      <a
        href={profile.contact.cv}
        class="inline-flex items-center gap-2 text-sm font-medium text-white/70 underline-offset-4 hover:text-white"
      >
        Download CV
        <span aria-hidden="true">↓</span>
      </a>
    </div>
    <div class="flex flex-wrap items-center justify-center gap-6 pt-4 text-sm text-white/60">
      {socialLinks.map((link) => (
        <a href={link.href} target="_blank" rel="noreferrer" class="transition hover:text-white">
          {link.label}
        </a>
      ))}
    </div>
  </div>
</section>
